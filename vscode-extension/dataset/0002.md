<events>
User edited "components/UserCard.tsx":

```diff
@@ -5,5 +5,5 @@
-}
-
-const UserCard: React.FC<UserCardProps> = ({ name, email, avatar }) => {
-    return (
-        <div className="user-card">
+    role?: string;
+    isActive?: boolean;
+}
+
+const UserCard: React.FC<UserCardProps> = ({ name, email, avatar, role, isActive = true }) => {
@@ -10,5 +10,5 @@
-            <img src={avatar} alt={name} />
-            <h3>{name}</h3>
-            <p>{email}</p>
-        </div>
-    );
+    return (
+        <div className={'user-card ' + (isActive ? 'active' : 'inactive')}>
+            <img src={avatar} alt={name} />
+            <h3>{name}</h3>
+            <p>{email}</p>
@@ -15,1 +15,4 @@
-};
+            {role && <span className="role">{role}</span>}
+        </div>
+    );
+};
```
</events>

<input>
```components/UserCard.tsx
<|editable_region_start|>
interface UserCardProps {
    name: string;
    email: string;<|user_cursor_is_here|>
    avatar: string;
}

<|editable_region_end|>
const UserCard: React.FC<UserCardProps> = ({ name, email, avatar }) => {
    return (
        <div className="user-card">
            <img src={avatar} alt={name} />
            <h3>{name}</h3>
            <p>{email}</p>
        </div>
    );
};
```
</input>

<output>
```components/UserCard.tsx
<|editable_region_start|>
interface UserCardProps {
    name: string;
    email: string;
    avatar: string;
    role?: string;
    isActive?: boolean;
<|editable_region_end|>
}

const UserCard: React.FC<UserCardProps> = ({ name, email, avatar, role, isActive = true }) => {
    return (
        <div className={'user-card ' + (isActive ? 'active' : 'inactive')}>
            <img src={avatar} alt={name} />
            <h3>{name}</h3>
            <p>{email}</p>
            {role && <span className="role">{role}</span>}
        </div>
    );
};
```
</output>

<labels>
non-local-edit,complete-pattern
</labels>